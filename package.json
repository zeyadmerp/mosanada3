{
  "name": "mosanada-website",
  "version": "1.0.0",
  "private": true,
  "description": "Mosanada corporate website — Arabic/English bilingual",
  "scripts": {
    "build": "node scripts/build.js",
    "favicons": "node scripts/generate-favicons.js",
    "split": "node ../scripts/split-html.js",
    "pages:deploy": "npx wrangler pages deploy . --project-name=mosanada3"
  },
  "devDependencies": {
    "sharp": "^0.34.3"
  }
}
