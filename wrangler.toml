# Cloudflare Workers — static site (HTML/CSS/JS)
#
# Deploy (from this folder):
#   npm install
#   npx wrangler login          # once
#   npm run deploy
#
# Or from parent folder (mosanda3):
#   npm run deploy

name = "mosanada3"
compatibility_date = "2024-08-01"

[assets]
directory = "dist"
html_handling = "auto-trailing-slash"
not_found_handling = "404-page"
