# Cloudflare Workers — static site (HTML/CSS/JS)
# Deploy command: npx wrangler deploy

name = "mosanada3"
compatibility_date = "2024-08-01"

[assets]
directory = "dist"
html_handling = "auto-trailing-slash"
not_found_handling = "404-page"
